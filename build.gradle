subprojects {
    version = '1.0'
}

task clean(type: Delete){
  delete 'bin'
}

task devclient(){
  dependsOn(":client:doBuild")
}

task devserver(){
  dependsOn(":server:doBuild")
}

task devbuild(){
  dependsOn devclient
  dependsOn devserver
}
